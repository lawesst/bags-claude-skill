# Embeddable Widgets

Copy‑paste widgets for Compound v3 dashboards. First up: utilization bar.

## Generate

```bash
npm install
npm run build
node dist/index.js
```

This writes `health-widget.html`. Drop it into any static page or iframe.

Next ideas: health for a wallet, borrow slider, “repay now” button. 